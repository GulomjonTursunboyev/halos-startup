services:
  solvo-bot:
    build: .
    container_name: solvo-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - DATABASE_PATH=data/solvo.db
    volumes:
      - ./data:/app/data
    networks:
      - solvo-network

networks:
  solvo-network:
    driver: bridge
